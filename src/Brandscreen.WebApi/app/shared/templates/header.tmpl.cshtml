<nav class="navbar navbar-inverse" ng-controller="HeaderController as vm">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar-collapse">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" rel="home" href="@Url.Action("Index", "Home", new {area = ""})" title="Brandscreen">
                <img style="height: 20px" src="@Url.Content("~/Content/Images/brzx_logo.png")" alt="Brandscreen"/>
            </a>
        </div>
        <div class="collapse navbar-collapse" id="navbar-collapse">
            <ul class="nav navbar-nav">
                <li>@Html.ActionLink("API", "Index", "Help", new {area = "HelpPage"}, null)</li>
                <li>
                    <a href="/manage">Manage</a>
                </li>
            </ul>
            <ul class="nav navbar-nav navbar-right">
                <li ng-if="vm.user.isAuthenticated">
                    <a href="/manage">Hello {{vm.user.name}}!</a>
                </li>
                <li ng-if="vm.user.isAuthenticated">
                    <a href="javascript:void(0)" ng-click="vm.logoff()">Log off</a>
                </li>
                <li ng-if="!vm.user.isAuthenticated">
                    @Html.ActionLink("Register", "Register", "Account", routeValues: new {area = ""}, htmlAttributes: new {id = "registerLink"})
                </li>
                <li ng-if="!vm.user.isAuthenticated">
                    @Html.ActionLink("Log in", "Login", "Account", routeValues: new {area = ""}, htmlAttributes: new {id = "loginLink"})
                </li>
            </ul>
        </div>
    </div>
</nav>